{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "nodemon index.js",
    "setup": "npm install && npm run setup-client",
    "setup-client": "cd .. && cd client && npm install && cd .. && cd server",
    "app": "concurrently -n api,client \"npm run server\" \"cd .. && cd client && npm run start\""
  },
  "author": "weefa",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "connect-mongodb-session": "^2.2.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-session": "^1.16.2",
    "mongoose": "^5.6.2",
    "setup": "0.0.3"
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "nodemon": "^1.19.1"
  }
}
